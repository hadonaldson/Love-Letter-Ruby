json.extract! contact, :id, :Name, :Wedding, :Date, :Getting, :Ready, :Location, :Service, :Type, :Number, :of, :Services, :Comments, :created_at, :updated_at
json.url contact_url(contact, format: :json)
